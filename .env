# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=scuser
DB_PASSWORD=scpass
DB_NAME=securechat

# Root DB creds (used for --init)
DB_ROOT_USER=root
DB_ROOT_PASS=rootpass

# Server config
SERVER_HOST=127.0.0.1
SERVER_PORT=9999

# Paths (do not commit secrets)
CERT_DIR=certs

# Logging / transcripts
TRANSCRIPT_DIR=transcripts

# Environment
ENV=development

